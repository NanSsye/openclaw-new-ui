# 2026.4.7 更新日志

## 协同工作台
- 新增 `/collab` 多 Agent 协同工作台页面
- 支持创建独立协作房间、选择主控 Agent 与多个 worker
- 支持聚合主控 / worker / 子会话的时间线视图
- 支持向主控发送补充指令、请求主控汇总、模板化派发任务
- 新增 Worker 状态面板、参与会话列表、本地历史快照说明
- 支持本地保存协作历史快照，刷新后仍可查看
- 支持历史任务归档、恢复、清空本地快照

## 聊天与交互
- 协作时间线底部按钮、下拉菜单、状态卡样式统一到聊天页风格
- 修复协作页 hydration mismatch、按钮嵌套、刷新后状态回退等问题
- 优化手机端布局：顶部压缩、三段切换、控制台折叠、房间列表紧凑化
- 优化空状态卡、过滤器、消息元信息在手机端的显示密度

## 构建与质量
- `npm run lint` 通过
- 保持协作页相关模块化结构，便于后续继续扩展
